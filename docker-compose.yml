version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: mini_coderspace
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: coderspace-db